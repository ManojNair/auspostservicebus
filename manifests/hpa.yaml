apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: deployapp
  labels:
    app.kubernetes.io/name: deployapp
    app.kubernetes.io/part-of: deployapp-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: deployapp
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80